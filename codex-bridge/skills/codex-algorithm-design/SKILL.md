---
name: codex-algorithm-design
description: >-
  Codexをアルゴリズム設計のセカンドオピニオンとして活用するスキル。
  "Codexにアルゴリズムを設計してもらう", "アルゴリズムのセカンドオピニオンが欲しい",
  "複雑なアルゴリズムの実装を比較したい", "Codexでアルゴリズム設計",
  "最適化のアイデアが欲しい" といったリクエストで使用する。
  Codexに独立したアルゴリズム実装を依頼し、計算量やアプローチを比較する。
version: 0.1.0
---

# Codexアルゴリズム設計スキル

## 概要

複雑なアルゴリズムの設計・実装フェーズでCodexをセカンドオピニオンとして活用する。
Claudeが自身のアルゴリズム案を作成した上で、Codexに独立した実装を依頼し、
両者のアプローチを比較してシフィさんに最適案を提示する。

## 実行手順

### 1. Claudeによるアルゴリズム設計

まずClaude自身が以下を作成（内部保持）:

- アルゴリズムの概要説明
- 計算量の分析（時間・空間）
- 擬似コードまたは実装
- エッジケースの考慮

### 2. Codexセッションの開始

`mcp__codex__codex` を呼び出す。developer-instructionsとプロンプトは `references/prompt-templates.md` を参照。

- **cwd**: 現在のプロジェクトディレクトリ
- **sandbox**: `workspace-write`（コード実行による検証を許可）

重要: Claudeのアルゴリズム案はCodexに渡さない（独立性を確保）。

### 3. 深掘り

`mcp__codex__codex-reply` で:
- 計算量の詳細分析を依頼
- エッジケースのテスト結果を確認
- 代替アプローチの有無を確認

### 4. アルゴリズムの比較と提示

```
═══ アルゴリズム設計結果（Claude + Codex セカンドオピニオン）═══

【問題定義】
  {problem_description}

【Claudeのアプローチ】
  手法: {approach}
  時間計算量: O(...)
  空間計算量: O(...)
  メリット: ...
  デメリット: ...
  [コード]

【Codexのアプローチ】
  手法: {approach}
  時間計算量: O(...)
  空間計算量: O(...)
  メリット: ...
  デメリット: ...
  [コード]

【比較表】
  | 観点 | Claude | Codex |
  |------|--------|-------|
  | 時間計算量 | ... | ... |
  | 空間計算量 | ... | ... |
  | 可読性 | ... | ... |
  | 実装の複雑さ | ... | ... |
  | エッジケース対応 | ... | ... |

【推奨】
  → {recommendation_or_escalation}
```

### 5. 最適案の選択

- 明確に優れている案がある場合は推奨する（根拠を明示）
- 同等の場合はシフィさんに判断を委ねる
- 両者の良い要素を組み合わせたハイブリッド実装が可能な場合は提案する

## 注意事項

- アルゴリズム設計ではCodexに `workspace-write` サンドボックスを許可し、コード実行テストを可能にする
- 計算量の主張が異なる場合は、具体的な入力例で検証する
- 既存コードベースの言語・スタイルに合わせた実装を提示する
